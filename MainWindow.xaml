<Window x:Class="Ulti.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ulti"
        mc:Ignorable="d"

        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"

        MaxHeight="390"
        MaxWidth="470"
        Height="350"
        Width="450"
        Background="Transparent">


    <!-- STÍLUSOK -->
    <Window.Resources>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Width" Value="170"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Background" Value="#3F3F3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#3A7AFE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2F66D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <!-- TARTALOM -->


    <Grid>
        <Border Background="#3A3A3A"
                CornerRadius="12"
                Padding="20"
                Margin="20"
                MouseLeftButtonDown="Window_MouseLeftButtonDown">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- CÍM -->
                <TextBlock Text="Játékosok nevei"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,15"/>

                <!-- ŰRLAP -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Első játékos:"/>
                    <TextBox x:Name="elso" Grid.Column="1"/>

                    <Label Content="Második játékos:" Grid.Row="1"/>
                    <TextBox x:Name="masodik" Grid.Row="1" Grid.Column="1"/>

                    <Label Content="Harmadik játékos:" Grid.Row="2"/>
                    <TextBox x:Name="harmadik" Grid.Row="2" Grid.Column="1"/>

                    <Label Content="Negyedik játékos:" Grid.Row="3"/>
                    <TextBox x:Name="negyedik" Grid.Row="3" Grid.Column="1"/>
                </Grid>

                <!-- ALSÓ SÁV -->
                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>


                    <Button x:Name="OKGOMB"
                            Grid.Column="1"
                            Content="OK"
                            Click="OKGOMB_Click"/>
                </Grid>

            </Grid>
        </Border>
        <Button Content="✕"
        Width="28"
        Height="28"
        Background="#AA3333"
        Foreground="White"
        HorizontalAlignment="Right"
        VerticalAlignment="Top"
        Margin="0,26,28,0"
        Click="Close_Click"/>
    </Grid>
</Window>
